# 武汉—上海远程光钟比对实验：综合项目报告

> 本报告综合整个项目两块工作：**引力红移分析**与**光纤链路**。具体包括：专业
> 30 秒综合差数据的使用、1550 nm 光纤链路与环外信号/温度分析、17 段光钟比对数据
> 的处理与相关性分析，以及对实验方 `潮汐修正后的比值计算.pdf` 内分析方法的独立
> 论证。面向希望完整理解「做了什么、得到什么、还差什么、下一步该怎么做」的读者，
> 其中相关性分析部分给出充分展开。

---

## 目录

1. 项目在做什么（一句话与背景）
2. 专业潮汐数据（30 秒综合差）
3. 实验数据处理：拍频 → 无跳点段 → 幅度/相关（数据处理）
4. 光钟比对：潮汐引力红移相关性分析（重点，充分解释）
5. 光纤链路：环外信号与天周期温度影响
6. PDF 分析处理部分的方法论证
7. 固定响应系数的潮汐修正（先修正、后算比值，独立结果）
8. 当前结论汇总
9. 建议：若保持精度实验，还需延长多久
10. 复现与文件索引

---

## 1. 项目在做什么（一句话与背景）

**一句话**：武汉与上海两地各有一台光学原子钟（Yb 和 Sr），通过 1550 nm 光纤链路
做**远程比对**。本项目围绕这条比对链路做两块分析——**(1) 引力红移**：用专业提供
的 30 秒间隔潮汐「综合差」ΔW，检验它是否通过**广义相对论引力红移**在比对频率差
里留下可检出的信号；**(2) 光纤链路**：分析 1550 nm 传递链路（武汉→合肥→上海）的
环外信号与温度影响。

**为什么值得做**：潮汐（日月引力 + 海水质量重新分布）会周期性地改变两地重力势，
幅度折算成频率差 Δf/f 约 **4.8×10⁻¹⁸**（rms）。这个量级恰好落在当前最先进光学钟
（精度 ~10⁻¹⁸）的分辨能力边缘——也就是说，**这是一项用真实原子钟验证广义相对论
潮汐效应**的实验。它的难点在于：信号太小，被链路噪声淹没约 3.6 倍，必须靠跨段
统计合并才能提取。

**实验格局**：1550 nm 传递链路 **武汉 → 合肥 → 上海**，实验分两轮（6 月与 8 月），
共 17 段无跳点数据；Yb 钟在武汉、Sr 钟在上海。

---

## 2. 专业潮汐数据（30 秒综合差）

专业人士提供了 30 秒间隔的武汉—上海潮汐结果（原始 Excel：
`clock/武汉-上海潮汐结果（0620-0910）-30秒间隔数据.xlsx`，UTC，覆盖 2026-06-20
至 09-10）。其表格含以下原始列：

| 列 | 含义 | 公式 |
|---|---|---|
| 固体潮（mm）CAS / SHA | 武汉 / 上海的固体潮径向位移 | — |
| 海潮（mm）CAS / SHA | 武汉 / 上海的海潮负荷位移 | — |
| 固体潮之差（mm） | 固体潮位移差 | CAS − SHA |
| 海潮之差（mm） | 海潮负荷位移差 | CAS − SHA |
| 综合差（mm） | 固体潮之差 + 海潮之差 | 上述两列之和 |

 本项目下游分析使用的潮汐数据为**「综合差」列**（固体潮之差 + 海潮之差，单位
mm）。该列按 `ΔW = g·(mm)/1000`（g ≈ 9.794 m/s²）换成引力势差单位 m²/s²，读数为
`results/professional_tidal_delta_30s.csv`（列名 `total_tidal_delta_m2_s2_surface`，
30 秒网格，239,040 行）。

**方向约定**：专业数据「之差」列的数值方向为 **CAS − SHA = 武汉 − 上海**（表头
公式 `= CAS − SHA` 的字面含义；上海沿海海潮负荷位移 std ≈ 26.6 mm 大于武汉内陆的
≈ 6.7 mm，故差值为负）。因此 ΔW = W(WUHN) − W(SHAO)。

---

## 3. 实验数据处理（拍频 → 无跳点段 → 幅度/相关）

实验数据处理是连接「原始拍频」与「潮汐检出结论」的中间层，也是本项目与实验方
`atomic-clock-comp.pdf` 最需要对齐的部分。核心链路如下：

```
原始拍频 Freq_B（1-s，北京时）  →  重建均匀时间轴  →  剔跳点  →  取最长无跳点段
   →  1200-s 三角窗积分（拍频与潮汐模板同窗投影）  →  幅度拟合 A / 相关 r
```

### 3.1 三个关键修正

1. **时区对齐**：原始拍频时间戳是北京时（UTC+8，文件头 `PC time, time zone local`），
   潮汐模板是 UTC。比对前把拍频窗口 `−8 h` 对齐到 UTC 潮汐时间轴。
2. **换算系数 COEF**：拍频[Hz] → 比值偏移的换算系数
   `COEF = 4.282082163269648e-15`（由 MATLAB `Dr` 公式推导），早期脚本误用
   `×F_BEAT`（错 7 个数量级），正确是 `beat = (Δf/f)/COEF`。
3. **均匀时间轴 + 剔跳点**：数据时间戳有 ±1 s 抖动（本质是均匀 1-s 采样），按
   MATLAB 约定「首点 + 均匀 1 s」重建时间轴；剔除偏离中位数 >10 Hz 的跳点；取剩余
   1-s 连续数据里的**最长无跳点段**（与 PDF「最长有效数据段 Teff」同一原则）。

### 3.2 幅度拟合与相关性（处理的目标输出）

对每段的拍频做 1200-s 三角（Bartlett）窗、600-s 步长积分，同时**潮汐模板也用
同一窗口投影**（先建 1-s 潮汐序列再同窗积分，而非窗口中心点采样），保证相关评估
公平；去均值后拟合 `beat = A·tide + noise`，得到幅度比 A 和相关系数 r。这两个数
是后续「潮汐是否被检出」判断的输入（详见 §4）。PDF 侧对应的是 OADEV 外推 + WLS
合并（详见 §6）。

---

## 4. 光钟比对：潮汐引力红移相关性分析（重点，充分解释）

### 4.1 物理量是怎么连起来的

潮汐势差 ΔW 通过引力红移改变两地钟的频率差：

```
Δf/f = ΔW / c²
```

也就是说，理论上两钟比对测到的相对频率差里，应该有一个**随时间按潮汐节律起伏**
的小信号。我们的任务就是：把这个理论预测的「潮汐波形模板」拿去和实测拍频比对，
看它们**相关性是否显著**。

### 4.2 什么是「相关性分析」（给不太懂统计的读者）

想象两条随时间波动的曲线：

- **实测曲线**：光钟比对拍频（含噪声，几乎看不出规律）；
- **理论曲线**：潮汐引力红移预测（形状确定，只是不知道实际出现多少）。

「相关性」问的就是：**这两条曲线是不是同起同落？** 如果是，说明潮汐信号确实
藏在实测数据里；如果毫无关联，说明测不到。

具体用两个数衡量：

1. **相关系数 r**：范围 −1 ~ +1。r 越接近 ±1，两条曲线越「同步」；r≈0 表示无关。
   - r 的正负号只说明方向（同向 or 反向），不影响「有没有关系」。
2. **p 值（显著性）**：回答「这个 r 是真实信号，还是纯噪声碰巧抖出来的？」。
   p 越小越可信，习惯上 p < 0.05 算「显著」。

### 4.3 为什么单段测不到、必须跨段合并（核心难点）

**关键矛盾**：潮汐信号太小了。

| 量 | Δf/f |
|---|---|
| 潮汐信号 rms | ~4.8×10⁻¹⁸ |
| 单段 1200-s 积分后噪声 | ~1.7×10⁻¹⁷（比信号大 ~3.6 倍） |
| 1-s 原始噪声 | ~1.9×10⁻¹⁵（比信号大 ~400 倍） |

单段的信噪比只有 **~0.28**——就像在嘈杂的收音机里听一句很轻的话，单听一段根本
听不清楚。所以单独看每一段，相关性大多**不显著**（这是必然的，不是失败）。

但噪声是「随机乱抖」（有正有负、彼此抵消），而潮汐信号有**确定的时间结构**
（每天该涨的时候涨、该落的时候落）。把 17 段独立的实验**合起来统计**，噪声
相互抵消，信号相干叠加，就能把单段看不见的信号「累」出来。

### 4.4 逐段与合并结果（最终值，专业综合差数据）

跨段合并后，相关性**极显著**：

| 检验 | 结果 |
|---|---|
| 负相关段数 | **14/17**（二项双侧 p=0.0127） |
| Stouffer 合并（符号无关） | \|z\| = **5.87**，p = 4.3×10⁻⁹ |
| Fisher 合并 | p = 2.2×10⁻⁶ |
| 加权相关系数 r | **−0.127**（弱相关） |
| 幅度比 A | **−0.54 ± 0.08**（距 0 约 6.4σ） |
| 会话层 Pearson r | +0.518（p=0.033，17 段，方向为正、已达 0.05 显著） |

### 4.5 这些数字分别意味着什么

- **「检出」已经成立**：合并后 p 值极小（10⁻⁹ 量级），说明潮汐信号**确实存在**，
  不是噪声巧合。这一点不依赖正负号方向。
- **信号「弱」但「真」**：r=−0.13 属弱相关，即潮汐数据只解释了拍频变异的 ~1.7%。
  这是合理的——因为噪声淹没信号 3.6 倍，本来就只能看到「趋势」，看不到完整波形。
  新增的第 15–17 段相关系数绝对值很小（|r| ≤ 0.03），稀释了合并 r，但符号一致性
  （14/17 同号）与显著性仍稳健。
- **幅度比 A=−0.54 是关键发现**：A=1 表示实测完整反映了理论潮汐幅度。实测只有
  **约一半**（−0.54），且方向一致为负。这提示：要么符号约定（s_beat）还没定死导致
  方向解读待确认，要么潮汐项在总链路里被部分抵消/稀释。
- **会话层 r=+0.518（p=0.033）**：17 个会话均值的正相关，方向对、已达 0.05 显著。
  这层用的 y_i 由本报告按 MATLAB 逐段比值公式（decimal 80 位）精确计算，
  `clock_ratio/compute_ratio.py` 输出 17 段完整值。

---

## 5. 光纤链路：环外信号与天周期温度影响

此前未分析的环外数据（计数器 4 通道 FXE_B1~B4）与三站气温（Meteostat：上海、
武汉、合肥）评估结果：

- **环外 10 MHz 信号（FXE_B2）**：约半数日子锁定，半数日子偶发失锁/跳变（单秒
  跳变可达数 MHz，是计数器相位平均失锁，**不是温度**）。
- **FXE_B4**（10 MHz 超稳参考）：std ~0.2 Hz，几乎无天周期。
- **天周期温度影响不可检出**：三站气温有明显日周期（上海日峰峰 4.6°C、武汉
  5.6°C，峰值 14–15 时），但环外参考/信号/1550 nm 链路拍频在锁定状态的日周期分量
  均远小于各自噪声（FXE_B8 鲁棒日峰峰仅 0.008 Hz）。
- **结论**：温度主要通过**链路时延/相位噪声**起作用，需专门的链路双向时延或机架
  温度日志才能量化；当前拍频数据不承载温度信息。**温度不影响潮汐检出结论**。

详细分析见 [clock/temperature/ANALYSIS.md](clock/temperature/ANALYSIS.md)。

---

## 6. PDF 分析处理部分的方法论证

> 本节是概要；对 `atomic-clock-comp.pdf` 分析处理部分（OADEV 外推、χ²、Birge、
> Mandel–Paule、Bayesian、跳点分析）的**完整逐项调研**见
> [archive/PDF_METHODOLOGY_REVIEW.md](archive/PDF_METHODOLOGY_REVIEW.md)（归档）。

`atomic-clock-comp.pdf` 是实验方的官方处理文档（Yb/Sr 比值的最佳估计），我们独立
核对了它的方法，并与本项目的潮汐分析对照，结论自洽。

### 6.1 PDF 的不确定度预算

| 项 | 值 |
|---|---|
| Sr 钟系统不确定度 u_Sr | 9.2×10⁻¹⁹ |
| Yb 钟系统不确定度 u_Yb | 1.1×10⁻¹⁸ |
| 高程差项 u_H | ~1×10⁻¹⁸（PDF 标注「待定」） |
| 统计不确定度（WLS） | 5.9×10⁻¹⁹ |
| 最终比值 | Yb/Sr = 1.207 507 039 343 337 72xx(23~24)，即 ~1.4×10⁻¹⁸ 相对精度 |

### 6.2 PDF 的三种统计方法（χ²、Birge、Mandel–Paule、Bayesian）

PDF 用三种方法处理「组间散布超过统计不确定度」的问题：

| 方法 | 假设 | 结果 |
|---|---|---|
| Birge ratio | 所有 u_i 按同一比例放大 | B=1.4156，u=8.35×10⁻¹⁹ |
| Mandel–Paule | 每个 bin 加相同 additive 方差 ξ² | ξ_MP=2.12×10⁻¹⁸，u=8.60×10⁻¹⁹ |
| Bayesian | ξ 为未知随机效应，传播其后验 | ξ_mean=2.59×10⁻¹⁸，u=9.98×10⁻¹⁹ |

### 6.3 关键论证：PDF 的「excess scatter」= 我们检出的「潮汐幅度被稀释」

这是本项目与 PDF 分析**相互印证的最重要一点**：

- PDF 的 **χ²_red = 2.004（p=0.0167）** 和 **Birge ratio = 1.4156** 都明确说明：
  **组间还有 OADEV 无法描述的随机过程**，误差棒低估了实际散布约 1.4 倍。
- 我们独立的潮汐相关性分析发现：**潮汐信号被噪声淹没 3.6 倍，实测幅度只有理论
  一半（A=−0.54）**，单段必然不显著，只有跨段合并才显著。

两条独立路径指向同一件事：**实验数据里存在一个确定结构的、幅度被噪声稀释的
时变信号**。PDF 把它归为「未建模随机噪声/excess scatter」，而我们把它具体化成了
「潮汐引力红移信号（检出、方向一致、约一半幅度）」。这为 PDF 里 Birge/M-P/Bayesian
估计的「额外散布 ξ≈2×10⁻¹⁸」提供了一个**物理候选来源**——至少部分可能来自潮汐项。

> 注意：PDF 的高程差项 u_H 标注「待定（?）」，而本项目的潮汐 ΔW 恰好贡献了这一
> 量级（ΔW/c² ~ 5×10⁻¹⁸ 量级，与 PDF 的 ξ 同量级）。这进一步支持「潮汐项是需要
> 显式建模的成分」这一判断。

---

## 7. 固定响应系数的潮汐修正（先修正、后算比值，独立结果）

前文 §4 的钟比值是**未做潮汐修正**的历史主结果。本节是新增的**独立情景分析**：
先对拍频做潮汐修正，再计算钟比值，并评估段内稳定度。它不覆盖、不替代 §4 的任何
结果与统计结论，二者并列。全精度数值与处理约定见
[clock_ratio/tidal_correction/summary.json](clock_ratio/tidal_correction/summary.json)；
汇总报告见 [clock_ratio/tidal_correction/REPORT.md](clock_ratio/tidal_correction/REPORT.md)。

### 7.1 两种修正情形与统一框架

潮汐影响按**两种情形**分开考虑，二者都基于同一份专家潮汐数据（30 秒「综合差」
`ΔW = W(武汉) − W(上海)`）与相对论引力红移关系 `Δf/f = ΔW/c²`：

| 情形 | 响应系数 A | 依据 |
|---|---|---|
| `raw`（基线，不修正） | 0 | 历史主结果，作对照 |
| `theory`（完全信任） | **−1** | 完全信任专家潮汐数据与相对论引力理论，按 −1 系数修正 |
| `empirical`（波形对、幅度错） | **−0.54** | 信任专家潮汐数据**波形正确、幅度不正确**，按 −0.54 参数修正 |

统一框架：拍频响应模型为 `b_raw = noise + A·h`，其中未去均值的潮汐拍频模板
`h = F_1550·ΔW/c²`（用 1550 nm 传递光频，非 1/COEF）。修正为
`b_corr = b_raw − A·h`，故 `theory`（A=−1）加回 h，`empirical`（A=−0.54）加回 0.54h；
负号只表示方向，不与模板预先反号重复。A 是**固定常数情景**，本次不重新拟合。

### 7.2 处理原则

- 三情景用**完全相同的原始保留样本**（17 组、1,008,912 个样本）与 `n_valid` 权重
  （每样本 1 s），冻结原始段窗口、扣除区间、去跳点、最长有效索引段与两端裁剪；
  修正后不重新筛选。
- 北京时间减 8 h 得 UTC，把 30 s 潮汐网格线性插值到实际保留样本；不外推、不钳位、
  不跨缺失区间插值。
- 在**小拍频偏差均值**上以 Decimal80 完成修正
  `mean_dm_corr = to_dec(raw_mean) − m_dec − A·to_dec(mean(h))`，再经完整 `full_ratio`
  反演（保留静态引力项 `DELTA_G` 与段系统频移 `shift_a`）。这是「先平均修正后的
  线性拍频、再反演」，不是平均瞬时比值；不向约 33 MHz 的 float64 载波直接叠加微小量。
- 段内稳定度用**真正重叠**、间断安全的 OADEV（每个非 1 s 时间步都断开，不跨段/
  跨间断拼接），对三情景一致的 τ 网格计算；参考统一为各段 raw 比值 R0，
  `y = −q/(1+q)` 保持无浮点相消。旧 `statistical_methods.oadev` 实为不重叠块均值，
  两者算法不同，数值差不作潮汐效应解释。

### 7.3 结果（时长加权钟比值）

`R_duration = Σ(n_valid_i·R_i)/Σn_valid_i`，三情景同一权重（非 WLS）；R 由完整
Decimal 按 ROUND_HALF_EVEN 舍入到小数点后 22 位，变化量从未舍入值计算。

| 情景 | A | R_duration | ΔR×10¹⁸（绝对比值差） | (R/R_raw−1)×10¹⁸（分数变化） |
|---|---|---|---|---|
| raw | 0 | 1.2075070393433377203696 | +0.000000 | +0.000000 |
| theory | -1 | 1.2075070393433377208108 | +0.441191 | +0.365374 |
| empirical | -0.54 | 1.2075070393433377206078 | +0.238243 | +0.197302 |

`ΔR = R_scenario − R_raw` 是带符号的**绝对比值差**（非绝对值）；分数变化须再除以
`R_raw`，二者不可混用，也不是潮汐 `mean_dff`（`mean(h)/F_1550`）。理论（A=−1）修正
使整个实验值上移约 +0.44×10⁻¹⁸；经验（A=−0.54）修正上移约 +0.24×10⁻¹⁸，约为前者
的 0.54 倍，方向一致。

### 7.4 段内稳定度（OADEV 比较）

对相同 τ、相同保留样本比较三情景 OADEV；倍数 <1 表示该固定情景下数值较低，
只是描述性结果，不代表统计显著或准确度提升。

| τ (s) | 情景 | 合格组数 | 倍数 < 1 | 倍数 ≥ 1 | 倍数范围 |
|---|---|---|---|---|---|
| 1200 | theory | 17 | 7 | 10 | 0.996782–1.002648 |
| 1200 | empirical | 17 | 10 | 7 | 0.998022–1.001392 |
| 3600 | theory | 17 | 13 | 4 | 0.932767–1.047307 |
| 3600 | empirical | 17 | 13 | 4 | 0.947917–1.013888 |
| 7200 | theory | 15 | 6 | 9 | 0.757745–1.427995 |
| 7200 | empirical | 15 | 10 | 5 | 0.789743–1.183188 |

长 τ（不可用）只算可比较的组，不填零。逐组全精度比值见
[ratio_scenarios.csv](clock_ratio/tidal_correction/ratio_scenarios.csv)，
全部 τ 与重叠对数见 [stability.csv](clock_ratio/tidal_correction/stability.csv)，方法见
[文档 §9](docs/METHODOLOGY.md#9-新增固定响应系数的潮汐修正与段内稳定度)。

### 7.5 方向自检：−0.54 修正后潮汐相关性消失

用「修正后潮汐相关性是否消失」自检 −0.54 的方向与幅度，绕开尚未定死的 s_beat
梳尺/本振符号链：沿历史 [batch_analysis.py](clock/segment_analysis/batch_analysis.py)
同一 1200-s 三角窗去均值处理链，对每段构造 `resid = beat − A_fix·tide` 再拟合。

| 量 | 修正前（raw） | 修正后（A_fix=−0.54） |
|---|---|---|
| 段均值幅度 A（未加权） | −0.340 | +0.200 |
| 段均值相关系数 r | −0.134 | **+0.0027** |
| 负相关段数 | 14/17（p=0.013） | 8/17（**p=1.000**） |
| Stouffer（带符号）z | −5.16 | **+0.39（p=0.69）** |
| Fisher 合并 p | 2.2×10⁻⁶ | **0.086** |

修正后 r 均值塌到 ≈0、带符号 Stouffer z 掉到 +0.39（不显著）、负相关段 8/17 随机；
网格扫描的残差相关性零点恰为 A_fix≈−0.54，与历史精度加权幅度 −0.5397±0.084 一致。
据此 −0.54 的符号方向与幅度都补对了。此自检只针对去均值波形（不含 DC），且残差
符号无关 |z|=4.33 与 Fisher p=0.086 仍略显著——反映残差里可能残留少量确定结构
（如段 14 单段 r=−0.21），但不改变「方向正确」的结论。

### 7.6 解释边界

- A=−0.54 来自历史**去均值波形**幅度，转用到未去均值模板的 DC（均值）部分属额外
  假设，不是校准，也不是本次重新估计的系数。
- 固定负号为指定响应约定，未新解决 [硬件极性](clock/SIGN_COEFFICIENT_ANALYSIS.md)。
- 未传播系数、潮汐模型或系统项不确定度；OADEV/样本标准差不是 SEM，不据此新建
  WLS、总不确定度或显著性检验，也不改变 §4 的历史统计结果。
- 不声称所有段改善；「OADEV 较低组数」只是指定 τ 下的描述性数值比较。

---

## 8. 当前结论汇总

| 维度 | 结论 |
|---|---|
| 潮汐数据 | 专业提供的 30 秒「综合差」，覆盖 2026-06-20 至 09-10（83 天） |
| 方向 | 武汉 − 上海（ΔW = W(WUHN) − W(SHAO)，即 CAS − SHA），无系统性符号错误 |
| 潮汐引力红移检出 | **确定为运算法则**（跨段合并 \|z\|=5.87，p=4.3×10⁻⁹） |
| 检出幅度 | 约为理论的一半（A=−0.54±0.08，6.4σ） |
| 方向 | 14/17 段一致为负；正负号物理方向待 s_beat 确认 |
| 温度影响 | 在现有拍频数据中不可检出，不影响潮汐结论 |
| PDF 自洽性 | Birge ratio 1.3 ≈ 我们检出的「幅度稀释」物理 |
| 潮汐修正（新情景，独立） | 先修正后算比值：theory(A=−1) 使 R_duration +0.44×10⁻¹⁸，empirical(A=−0.54) +0.24×10⁻¹⁸；段内 OADEV 变化为描述性结果，不改变历史基线 |

---

## 9. 建议：若保持精度实验，还需延长多久

### 9.1 关键约束：积分窗口的「平台」是潮汐信号，不是噪声

噪声模型（经确认）：**光纤链路是白相位噪声（Allan σ ∝ 1/τ），钟是白频率噪声
（σ ∝ 1/√τ）**。链路白相位噪声的频率 PSD ∝ f²，在潮汐超低频段（~2×10⁻⁵ Hz）
几乎为零，不构成潮汐检出的本底；真正的本底是**钟的白频率噪声**（频率 PSD 平坦）。

组 13 的 Allan 方差在 τ ≳ 8000 s 后「趋平」，先前误归因于链路闪烁（1/f）噪声；
实测检验表明那是**潮汐信号本身（12.4 h / 24 h 确定性周期）+ 缓慢温漂**在长 τ 下
顶住了方差——扣除潮汐模板后斜率恢复到白频率的 −0.5。因此：

> 加大单段积分窗口并不免费：窗口越大，潮汐是振荡信号，其幅度信息被窗口平均
> 衰减（7200 s 时方差保留仅 ~0.92），且独立点数急剧减少。段 13 实测幅度比不确定
> 度 u_A 在窗口 300–1200 s 基本持平（~0.23–0.25），到 7200 s 反而恶化到 0.31。
> 故 1200-s 窗口是「噪声压制」与「独立点数」之间的经验平衡点，而非唯一解。

### 9.2 唯一有效的延长方式：增加独立实验段数

潮汐信号是确定的（每个不同日期给出独立的潮汐相位实现），跨天合并可以让噪声
抵消、信号相干累加。幅度比 A 的精度随独立段数 N 按 **1/√N** 提升：

- 现状：A = 0.54 ± 0.08（17 段，相对精度 15%）。

| 目标（幅度比相对精度） | 所需独立段数 | 连续运行估算 |
|---:|---:|---:|
| 10%（钉死「约一半」） | ~38 段 | ~38 天 |
| 5%（区分 0.5 vs 0.6） | ~150 段 | ~150 天（约 5 个月） |
| 3%（A=0.54±0.016） | ~416 段 | ~416 天（约 1.1 年） |

> 注：当前 17 段横跨约 56 天（6 月最后几日 + 7 月初 + 8 月两轮，含空档），有效段率
> 约 0.3 段/天；若连续运行无空档，每天约 1 段，上表时间即连续运行天数。

### 9.3 更高性价比的改进方向（比单纯延长更重要）

按「单位时长产出」排序，建议优先级：

1. **定死 s_beat 符号**（0 成本）：确认梳尺/本振频率高低，即可确定潮汐效应物理
   方向，把「方向待定」变成「方向已确认」，产出立竿见影。
2. **用两钟直接频率比对数据替代 1550 nm 传递链路拍频**：真正的噪声本底是钟的
   白频率噪声（1/√τ），链路白相位噪声（1/τ）只影响短时标；若能用 Yb/Sr 直接
   比对（绕过链路附加的相位噪声），同样的实验时长能提升幅度精度。
3. **采集链路双向时延/机架温度日志**：把温度-链路耦合这个「未建模项」显式量化，
   可能解释幅度 <1 的部分来源。
4. **确认高程差项 u_H**：PDF 标注「待定」的 1×10⁻¹⁸ 项，与本项目潮汐 ΔW 同量级，
   值得显式核算，避免它和潮汐项混淆。
5. **累积 ~33 段（约 1 个月连续）**：先把幅度比「是不是一半」从 15% 精度压到 10%，
   这是性价比最高的延长段位；再往后（5%、3%）边际收益递减，需与上述改进结合。

### 9.4 一句话结论

**保持现有精度与链路，延长单段时间无效；真正的产出来自「增加独立实验段数」
（约 1 个月连续可把幅度精度从 15% 提到 10%）＋「定死 s_beat 符号」＋「改用
更低噪声的两钟直接比对」，三者结合才能把「潮汐幅度约为理论一半」这一发现
坐实为高置信的定量结论。**

---

## 10. 复现与文件索引

```bash
# 一键分析 + 出报告（推荐）
python run_all.py

# 或分步
python clock_ratio/compute_ratio.py              # 17 段钟比值（含端点筛选）
python clock/segment_analysis/batch_analysis.py   # 17 段批量分析 + 跨段合并统计（核心）
python clock/clock_tidal_shift.py                 # 17 组会话平均潮汐频差
python clock_ratio/correlation_reanalysis.py      # 段均值相关（y_i vs Δf/f）+ 加权均值 + 修正量
python clock_ratio/make_report.py                 # 自动生成权威报告

# 新增：固定响应系数的潮汐修正（先修正、后算比值，独立结果；见 §7）
python run_all.py --tidal-only                    # 只重建独立潮汐分析与报告，不碰旧产物
python clock_ratio/tidal_correction.py && python clock_ratio/make_tidal_report.py  # 同上，分步

# 温度
python clock/temperature/fetch_temperature.py
```

> 完整流程、参数说明、实验条件改变时改哪里见
> [docs/WORKFLOW.md](docs/WORKFLOW.md)。

| 文件 | 内容 |
|---|---|
| [README.md](README.md) | 项目总览 + 符号表 |
| [PROJECT_REPORT.md](PROJECT_REPORT.md) | 综合项目报告（本文件） |
| [clock_ratio/EXPERIMENT_REPORT.md](clock_ratio/EXPERIMENT_REPORT.md) | **总权威报告**（钟比值+潮汐+相关性，自动生成；保留未做潮汐修正的旧基线） |
| [clock_ratio/tidal_correction/REPORT.md](clock_ratio/tidal_correction/REPORT.md) | **独立潮汐修正报告**（raw/theory/empirical 三情景，先修正后算比值 + 段内稳定度） |
| [clock_ratio/tidal_correction/](clock_ratio/tidal_correction/) | 潮汐修正独立产物：summary.json / ratio_scenarios.csv / stability.csv / REPORT.md |
| [docs/WORKFLOW.md](docs/WORKFLOW.md) | 总流程文档（怎么跑、实验条件变了改哪） |
| [docs/NOTATION.md](docs/NOTATION.md) | 符号与术语表 |
| [docs/ERROR_CHECKLIST.md](docs/ERROR_CHECKLIST.md) | 错误清单 + 检查项目 |
| [clock/params.json](clock/params.json) + [clock/PARAMS.md](clock/PARAMS.md) | 中间参数文档 + 字段说明 |
| [clock/shared.py](clock/shared.py) | 常量/段定义/数据加载单一真源（读 params.json） |
| [run_all.py](run_all.py) | 一键分析入口 |
| [clock/SIGN_COEFFICIENT_ANALYSIS.md](clock/SIGN_COEFFICIENT_ANALYSIS.md) | 符号系数提取 |
| [clock/PROFESSIONAL_TIDAL_DATA.md](clock/PROFESSIONAL_TIDAL_DATA.md) | 专业潮汐数据说明 |
| [clock/temperature/ANALYSIS.md](clock/temperature/ANALYSIS.md) | 环外+温度分析 |
| [archive/](archive/README.md) | 历史/重叠/过时文档（旧 14 段、旧 A≈−0.45） |
| [results/professional_tidal_delta_30s.csv](results/professional_tidal_delta_30s.csv) | 专业综合差 ΔW 数据（下游分析输入） |
| `clock/潮汐修正后的比值计算.pdf` | 实验方 17 段修订版处理文档 |

> 原始实验数据与 MATLAB 程序在 `clock/data/`，已 gitignore，不上传。
